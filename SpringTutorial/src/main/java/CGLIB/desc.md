byte code library динамичеки меняет уже сформированный байт код


(например добавить метод 
"to string" ко всем
классам у которых есть поле name)

все это нужно, чтобы не писать статичный код, а сгенерировать на лету
